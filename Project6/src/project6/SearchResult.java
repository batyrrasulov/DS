package project6;

// store the search results and the number of nodes inspected
public class SearchResult {
	boolean found;
	int inspected;
	public SearchResult(boolean found, int inspected) {
		this.found = found;
		this.inspected = inspected;
		}
	}
